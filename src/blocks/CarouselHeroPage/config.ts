import { Block } from "payload";

export const CarouselHeroPage: Block = {
    slug: 'carouselHeroPage',
    interfaceName: 'CarouselHeroPage',
    fields: [
        {
            name: 'carousel',
            type: 'ui',
            admin: {
            },
        }
    ],
} 